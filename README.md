# python_webapp
This repository is my first attempto create a python web application


The application is a web app.  avery simple web app that check if a credit card number is valid or not using lhuns algorithm
